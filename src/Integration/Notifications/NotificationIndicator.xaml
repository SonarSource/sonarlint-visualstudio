<UserControl x:Class="SonarLint.VisualStudio.Integration.Notifications.NotificationIndicator"
             x:Name="UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SonarLint.VisualStudio.Integration.Notifications">
    <Grid>
        <ToggleButton x:Name="PART_Button" ClickMode="Press">
            <ToggleButton.Template>
                <ControlTemplate>
                    <Grid>
                        <Border x:Name="PART_Border"
                                BorderBrush="Gray" Width="16" Height="16" BorderThickness="1" Background="White" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding HasEvents}" Value="True">
                            <Setter Value="Red" Property="BorderBrush" TargetName="PART_Border"  />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>
        <Popup IsOpen="{Binding IsChecked, ElementName=PART_Button, Mode=TwoWay}"
               PlacementTarget="{Binding ElementName=PART_Button}"
               Placement="Top" StaysOpen="False" PopupAnimation="Fade">
            <Grid>
                <Border BorderThickness="1" BorderBrush="Red" Background="White" Width="360" Height="80" />
                <TextBlock Text="{Binding BalloonTipText}" TextWrapping="Wrap" />
                <Button Click="ShowMore_Click" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="3">
                    <Button.Template>
                        <ControlTemplate>
                            <TextBlock>
                                <Hyperlink>Show more</Hyperlink>
                            </TextBlock>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Width="15" Height="15" Padding="0,-2,0,0" Margin="5"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Click="Close_Click">X</Button>
            </Grid>
        </Popup>
    </Grid>
</UserControl>
