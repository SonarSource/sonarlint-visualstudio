<UserControl x:Class="SonarLint.VisualStudio.Integration.Vsix.GeneralOptionsDialogControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">

    <!--  #Design  -->
    <StackPanel Margin="5,0">
        <Grid x:Name="ActivateMoreGrid" Margin="0,0,0,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Activate more..." Click="OnActivateMoreClicked"
                    Margin="5" Padding="5,2.5" />
            <TextBlock Grid.Column="1"
                       Text="Install and activate JavaScript support."
                       Margin="5" TextWrapping="Wrap"/>

            <Button Grid.Column="0" Grid.Row="1" Content="Deactivate" Click="OnDeactivateClicked"
                    Margin="5" Padding="5,2.5" />
            <TextBlock Grid.Column="1" Grid.Row="1"
                       Text="Deactivate JavaScript support."
                       Margin="5" TextWrapping="Wrap"/>

            <TextBlock Grid.Column="1" Grid.Row="2"
                       Text="SonarLint for Visual Studio can also analyze JavaScript files. After installing JavaScript support, it will be activated for newly opened files. Note that it doesn't benefit from the connected mode for now."
                       Margin="5" TextWrapping="Wrap" />
        </Grid>

        <CheckBox x:Name="ShowServerNuGetTrustWarning"
                  Content="Display a warning about NuGet packages" />
        <TextBlock Margin="20,5,10,5"
                   Text="Display a warning about automatically downloading NuGet packages when establishing a SonarQube server connection."
                   TextWrapping="Wrap" />

    </StackPanel>
</UserControl>
