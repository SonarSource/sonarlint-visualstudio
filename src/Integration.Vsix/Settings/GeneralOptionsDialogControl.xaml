<UserControl x:Class="SonarLint.VisualStudio.Integration.Vsix.GeneralOptionsDialogControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">

    <!--  #Design  -->
    <StackPanel Margin="5,0">
        <CheckBox x:Name="ShowServerNuGetTrustWarning"
                  Margin="10,0"
                  Content="Display a warning about NuGet packages" />
        <TextBlock Margin="30,5,10,5"
                   Text="Display a warning about automatically downloading NuGet packages when establishing a SonarQube server connection."
                   TextWrapping="Wrap" />
        <!--<CheckBox x:Name="EnableJavascriptAnalysis"
                  Margin="10,0"
                  Content="Enable JavaScript analysis" />
        <StackPanel Orientation="Horizontal" Margin="30,5,10,5">
            <TextBlock Text="Some text" />
            <Button Margin="20,0,0,0" Content="Download" Click="OnDownloadButtonClicked" />
        </StackPanel>-->
    </StackPanel>
</UserControl>
