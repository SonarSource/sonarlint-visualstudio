<Window x:Class="SonarLint.VisualStudio.Integration.Vsix.Commands.DebugMenu.RuleIdInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wpf="clr-namespace:SonarLint.VisualStudio.Core.WPF;assembly=SonarLint.VisualStudio.Core"
        Title="Open Rule Description"
        Width="400"
        Height="180"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        x:ClassModifier="internal"
        x:Name="This">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/SonarLint.VisualStudio.ConnectedMode;component/UI/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Button">
                <Setter Property="MinHeight" Value="25" />
                <Setter Property="MinWidth" Value="75" />
                <Setter Property="Margin" Value="10,0,0,0" />
                <Setter Property="Padding" Value="5,2"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Height" Value="30" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
            <wpf:BoolToVisibilityConverter x:Key="TrueToVisibleConverter" FalseValue="Hidden" TrueValue="Visible"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid DataContext="{Binding ElementName=This, Path=ViewModel}" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Enter rule ID (e.g., csharpsquid:S1000):" />
            <TextBox Grid.Row="1" Margin="0,5"
                     Text="{Binding Path=RuleId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" />
            <TextBlock Grid.Row="2" Margin="0,5,0,0"
                       Text="{Binding Path=ValidationMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       Visibility="{Binding Path=IsValidRuleId, Converter={StaticResource TrueToVisibleConverter}}" />
        </Grid>

        <Grid Grid.Row="1" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="OK" IsDefault="True"
                    IsEnabled="{Binding Path=IsValidRuleId}"
                    Click="OkButton_OnClick"/>
            <Button Grid.Column="1" Content="Cancel" IsCancel="True" />
        </Grid>
    </Grid>
</Window>
